<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.webtoon.dao.member.IMemberDao">

    <insert id="insertMember" parameterType="com.example.webtoon.dto.member.MemberVO">
        insert into member( userid, pwd, name, email, phone, zip_num, address1, address2, address3, gender)
        values( #{userid}, #{pwd}, #{name}, #{email}, #{phone}, #{zip_num}, #{address1}, #{address2}, #{address3}, #{gender})
    </insert>

    <select id="getMember" resultType="com.example.webtoon.dto.member.MemberVO" >
        select * from member where userid=#{param1}
    </select>


</mapper>